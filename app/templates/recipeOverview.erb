<nav class="navbar navbar-default" role="navigation">
    <p class="navbar-text">Rezepte (<%= numRecipes %>)</p>
    <form class="navbar-form navbar-right" role="search">
        <div class="form-group">
            <input id="search" type="text" class="form-control" placeholder="Search">
        </div>
    </form>
</nav>
<div class="table-responsive">
    <table id="recipeTable" class="table overviewTable">
        <tr>
            <th class="name <% if(orderBy === 'name') { print('active'); } %>">Name <span class="glyphicon glyphicon-sort-by-alphabet<% if(invertOrder) { %>-alt<% } %>"></span></th>
            <th class="created <% if(orderBy === 'created') { print('active'); } %>">Eingefügt <span class="glyphicon glyphicon-sort-by-attributes<% if(invertOrder) { print('-alt') } %>"></span></th>
            <th class="views <% if(orderBy === 'views') { print('active'); } %>">Views <span class="glyphicon glyphicon-sort-by-attributes<% if(invertOrder) { print('-alt') } %>"></span></th>
            <th class="activeId <% if(orderBy === 'activeId') { print('active'); } %>">Status <span class="glyphicon glyphicon-sort-by-attributes<% if(invertOrder) { print('-alt') } %>"></span></th>
            <th></th>
        </tr>
    </table>
</div>
